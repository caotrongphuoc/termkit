#!/usr/bin/env bash
# Apply step: read settings.conf and write the managed block into ~/.bashrc.
# Filled in during Part 6.

# ----- public API (to be implemented) -----------------------------------------
# apply_all           -> runs the full apply pipeline
# apply_uninstall     -> removes the managed block and restores backup (Part 7)
